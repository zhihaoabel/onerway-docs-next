export default defineAppConfig({
  ui: {
    colors: {
      primary: "indigo"
    }
  },

  // 滚动条会自动使用这些颜色
  scrollbar: {
    primary: true, // 使用 primary 颜色
    thickness: "normal" // thin | normal | thick
  }
});
